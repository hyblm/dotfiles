# dotfiles

Managed with [`dotter`](https://github.com/SuperCuber/dotter/wiki/1.-Getting-Started).
The linux and windows binaries are hosted in repo.

## Setup

Create `.dotter/local.toml` and specify which packages you want.

```toml
packages = ["default", "nu", "zed", "nvim"]
```
